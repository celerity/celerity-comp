#pragma once

#include <llvm/ADT/StringMap.h>
using namespace llvm;

namespace celerity {
/*
    StringMap<Value*> checkKernelInvariants(llvm::Function &fun){ 

        StringMap<Value*> invariants;

// 1. findKernel from Module

// 2. check if a varialbe (Value) is globalSize or LocalSize os subgroups ...

// 3.  check if a varialbe is an int argument

        return 
    }
*/

}